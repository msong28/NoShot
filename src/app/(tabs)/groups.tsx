import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function GroupsScreen() {
  return (
    <PlaceholderScreen
      title="Groups"
      description="Your groups, with per-currency net indicators and active-bet counts, will live here."
    />
  );
}
