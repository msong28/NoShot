import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function AccountScreen() {
  return (
    <PlaceholderScreen
      title="Account"
      description="Username, blocked users, privacy settings, delete account, and sign out will live here."
    />
  );
}
